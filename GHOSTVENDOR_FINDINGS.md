# GhostVendor Resilience Findings

> 🤖 Generated by [GhostVendor](https://github.com/nithinbr33/ghostvendor) — autonomous dependency resilience engineer.
> ⚠️ Pipeline did not complete patch validation. This PR contains findings only — no code changes.

## Resilience Score: 15/100

## Failed Scenarios
- **Stripe**: 502_burst, timeout, 429_rate_limit, malformed_json
- **SendGrid**: timeout, 502_burst, 429_rate_limit, empty_response

## Root Cause Diagnoses
- **Stripe** (`src/app/clients/stripe.py` → `create_payment_intent`): circuit_breaker — The Stripe client in src/app/clients/stripe.py create_payment_intent() makes a requests.post() call with no timeout parameter (missing_timeout), causing indefinite hangs on slow vendors. It does not check response.status_code before calling response.json(), so vendor 502/429 errors trigger JSON decode exceptions that propagate unhandled (no_error_propagation). There is no retry logic for transient 502/429 responses (missing_retry) and no circuit breaker to stop hammering a failing vendor (missing_circuit_breaker). The response.json() call is unguarded against malformed or empty bodies (json_parse_unguarded, empty_response_unguarded). The charge route in src/app/routes/charge.py charge() simply returns the client result, so any exception becomes an unhandled 500 to the caller.
- **SendGrid** (`src/app/clients/sendgrid.py` → `send_email`): timeout_with_retry — The send_email function in src/app/clients/sendgrid.py makes a requests.post() call at line 27-37 without any timeout parameter, causing the HTTP request to hang indefinitely when SendGrid is slow or unresponsive. All four chaos modes (timeout, 502_burst, 429_rate_limit, empty_response) manifest as requests.exceptions.Timeout because the vendor simulator delays responses beyond the client's default wait time. Additionally, there is no retry logic for transient errors (429, 502) and no circuit breaker to prevent repeated calls to a failing vendor. The notify route handler in src/app/routes/notify.py simply returns the status code, so any exception propagates as an unhandled 500 to the caller.

## Attempted Patches
- **Stripe**: fix(stripe): add circuit breaker with timeout and retry to create_payment_intent

## What Failed
Stripe requests hung indefinitely with no timeout, 502/429 errors caused JSON decode crashes, and repeated failures were never stopped.
## Fix Applied
Added a circuit breaker that opens after 3 consecutive failures and resets after 60 seconds, a 10-second timeout, one retry on 429/502/503, and structured error dicts on all failure paths.
## Why This Strategy
Stripe is criticality 95 so aggressive resilience is required; the circuit breaker prevents cascading failures while timeout and retry handle transient issues gracefully.
- **SendGrid**: fix(sendgrid): add timeout and retry to send_email with caller guard

## What Failed
SendGrid HTTP calls had no timeout, causing indefinite hangs when the vendor was slow or unresponsive.
## Fix Applied
Added a 10-second timeout and one retry on transient errors (429, 502, 503) with a 1-second sleep between attempts.
## Why This Strategy
Timeout with retry prevents hanging while allowing one recovery attempt for transient failures, matching the moderate criticality of the vendor.

## Failure Reason
```
Stripe/502_burst: still returning HTTP 500 after patch (unchanged); Stripe/429_rate_limit: still timing out after patch; Stripe/malformed_json: still timing out after patch; SendGrid: patched app failed to start: HTTPConnectionPool(host='localhost', port=8002): Read timed out. (read timeout=5)
```

## Recommended Next Steps
Review the diagnoses above and apply the suggested fix strategies manually.
