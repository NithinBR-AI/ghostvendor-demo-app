# GhostVendor Resilience Findings

> 🤖 Generated by [GhostVendor](https://github.com/nithinbr33/ghostvendor) — autonomous dependency resilience engineer.
> ⚠️ Pipeline did not complete patch validation. This PR contains findings only — no code changes.

## Resilience Score: 10/100

## Failed Scenarios
- **Stripe**: timeout, 502_burst, 429_rate_limit, malformed_json, empty_response
- **SendGrid**: 429_rate_limit, timeout, 502_burst

## Root Cause Diagnoses
- **Stripe** (`src/app/clients/stripe.py` → `create_payment_intent`): timeout_with_retry — In src/app/clients/stripe.py, create_payment_intent() calls requests.post() with no timeout parameter, causing the HTTP request to hang indefinitely when Stripe is slow or unresponsive â€” this explains why all five chaos modes (timeout, 502_burst, 429_rate_limit, malformed_json, empty_response) manifest as requests.exceptions.Timeout in the test harness. Additionally, the function returns response.json() directly without checking response.status_code (so 4xx/5xx errors are swallowed and parsed as JSON), without try/except around .json() (so malformed or empty bodies raise uncaught JSONDecodeError), and with no retry logic for transient 429/502 responses or circuit breaker to stop hammering a failing vendor. The route handler in src/app/routes/charge.py calls this function and lets any exception propagate as an unhandled 500.
- **SendGrid** (`src/app/clients/sendgrid.py` → `send_email`): timeout_with_retry — The send_email function in src/app/clients/sendgrid.py calls requests.post() without a timeout parameter (lines 30-40), causing a 30-second hang when SendGrid is slow (timeout scenario: 30016ms vs 15ms baseline). The function returns response.status_code directly without validating it, so vendor error codes 429 (rate limit) and 502 (bad gateway) propagate as successful return values. The notify route in src/app/routes/notify.py (lines 25-30) unconditionally returns HTTP 200 with {"status": "sent", "code": <vendor_code>}, silently swallowing vendor failures as "sent" successes. No retry logic exists for transient 429/502 errors, and no circuit breaker prevents repeated calls to a degraded vendor.

## Attempted Patches
- None generated

## Failure Reason
```
Agent 6 (Patch Generator) failed: Agent 6 returned invalid JSON for Stripe: Expecting ',' delimiter: line 10 column 3 (char 4528)

Raw:
{
  "vendor": "Stripe",
  "affected_file": "src/app/clients/stripe.py",
  "fix_strategy": "timeout_with_retry",
  "fixed_source": "\"\"\"Stripe API client \u2014 intentionally has no retry, timeout, or error handling.\"\"\"\n\nimport os\nimport time\nimport requests\nfrom requests.exceptions import Timeout, RequestException\nimport json\n\n\ndef create_payment_intent(amount: int, currency: str, payment_method: str) -> dict:\n    \"\"\"\n    Create and confirm a Stripe PaymentIntent.\n\n    Args:\n        amount: Amount in smallest currency unit (e.g. cents for USD).\n        currency: ISO 4217 currency code (e.g. \"usd\").\n        payment_method: Stripe PaymentMethod ID (e.g. \"pm_card_visa\").\n\n    Returns:\n        Raw Stripe API response as a dict.\n\n    Raises:\n        KeyError: If the response does not contain expected fields (no validation).\n        requests.exceptions.Timeout: Propagates uncaught if Stripe times out.\n    \"\"\"\n    base_url = os.environ.get(\"STRIPE_BASE_URL\", \"https://api.stripe.com\")\n    api_key = os.environ[\"STRIPE_API_KEY\"]\n\n    max_attempts = 2\n    for attempt in range(max_attempts):\n        try:\n            response = requests.post(\n                f\"{base_url}/v1/payment_intents\",\n                auth=(api_key, \"\"),\n                data={\n                    \"amount\": amount,\n                    \"currency\": currency,\n                    \"payment_method\": payment_method,\n                    \"confirm\": True,\n                },\n                timeout=10,\n            )\n        except Timeout:\n            if attempt == max_attempts - 1:\n                return {\"error\": \"timeout\", \"message\": \"Stripe request timed out after 10 seconds\"}\n            time.sleep(1)\n            continue\n        except RequestException as e:\n            if attempt == max_attempts - 1:\n                return {\"error\": \"request_exception\", \"message\": str(e)}\n            time.sleep(1)\n            continue\n\n        # Check for transient HTTP status codes\n        if response.status_code in (429, 502, 503):\n            if attempt == max_attempts - 1:\n                try:\n                    error_body = response.json()\n                except json.JSONDecodeError:\n                    error_body = response.text\n                return {\"error\": \"transient_http_error\", \"message\": f\"Stripe returned {response.status_code}: {error_body}\"}\n            time.sleep(1)\n            continue\n\n        # Non-transient error status\n        if not (200 <= response.status_code < 300):\n            try:\n                error_body = response.json()\n            except json.JSONDecodeError:\n                error_body = response.text\n            return {\"error\": \"http_error\", \"message\": f\"Stripe returned {response.status_code}: {error_body}\"}\n\n        # Successful response - parse JSON\n        try:\n            return response.json()\n        except json.JSONDecodeError:\n            if attempt == max_attempts - 1:\n                return {\"error\": \"malformed_json\", \"message\": \"Stripe response is not valid JSON\"}\n            time.sleep(1)\n            continue\n\n    # Should not reach here\n    return {\"error\": \"unknown\", \"message\": \"Unexpected exit from retry loop\"}\n",
  "extra_patches": [
    {
      "affected_file": "src/app/routes/charge.py",
      "fixed_source": "\"\"\"POST /charge \u2014 process a payment via Stripe.\"\"\"\n\nfrom flask import Blueprint, request, jsonify\nfrom app.clients import stripe\n\ncharge_bp = Blueprint(\"charge\", __name__)\n\n\n@charge_bp.route(\"/charge\", methods=[\"POST\"])\ndef charge():\n    \"\"\"\n    Process a payment intent via Stripe.\n\n    Request JSON:\n        amount (int): Amount in cents.\n        currency (str, optional): ISO 4217 code. Defaults to \"usd\".\n        payment_method (str): Stripe PaymentMethod ID.\n\n    Returns:\n        JSON with payment_intent_id and status on success.\n        No error handling \u2014 any Stripe failure propagates as an unhandled exception.\n    \"\"\"\n    data = request.json\n    result = stripe.create_payment_intent(\n        amount=data[\"amount\"],\n        currency=data.get(\"currency\", \"usd\"),\n        payment_method=data[\"payment_method\"],\n    )\n    if \"error\" in result:\n        return jsonify({\"error\": result.get(\"message\", \"vendor error\")}), 503\n    return jsonify({\"payment_intent_id\": result[\"id\"], \"status\": result[\"status\"]})\n"
  ],
  "pr_title": "fix(stripe): add timeout, retry, and error handling to create_payment_intent",
  "pr_description": "## What Failed\nThe `create_payment_intent` function in `src/app/clients/stripe.py` called `requests.post` without a timeout, causing indefinite hangs. It also lacked retry logic for transient errors (429, 502, 503), did not check HTTP status codes, and returned `response.json()` without guarding against malformed or empty JSON. The caller in `src/app/routes/charge.py` accessed `result[\"id\"]` and `result[\"status\"]` directly, so any exception or error dict would cause a KeyError and an unhandled 500.\n\n## Fix Applied\n- Added `timeout=10` to the HTTP request.\n- Wrapped the call in a retry loop (max 2 attempts) with a 1-second sleep between attempts.\n- Catch `Timeout` and `RequestException`; return structured error dicts.\n- Retry on transient status codes (429, 502, 503); return error dict after final attempt.\n- Check for non-2xx status codes and return error dict.\n- Guard `response.json()` with try/except for `JSONDecodeError`; retry once then return error dict.\n- Patched the caller `charge.py` to check `if \"error\" in result:` before accessing fields, returning a 503 with the error message.\n\n## Why This Strategy\nThe `timeout_with_retry` strategy directly addresses the root cause: missing timeout, missing retry, and unguarded JSON parsing. It ensures the function never hangs indefinitely, retries transient failures once, and always returns a dict (either success or structured error) so callers can handle failures gracefully. The caller patch prevents KeyError crashes and surfaces vendor errors as 503 responses."
}
```

## Recommended Next Steps
Review the diagnoses above and apply the suggested fix strategies manually.
