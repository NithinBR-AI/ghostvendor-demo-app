# GhostVendor Resilience Findings

> 🤖 Generated by [GhostVendor](https://github.com/nithinbr33/ghostvendor) — autonomous dependency resilience engineer.
> ⚠️ Pipeline did not complete patch validation. This PR contains findings only — no code changes.

## Resilience Score: 23/100

## Failed Scenarios
- **Stripe**: timeout, 502_burst, 429_rate_limit, 502_burst, 429_rate_limit
- **SendGrid**: 429_rate_limit, 502_burst, timeout

## Root Cause Diagnoses
- **Stripe** (`src/app/clients/stripe.py` → `create_payment_intent`): timeout_with_retry — The Stripe client in src/app/clients/stripe.py function create_payment_intent() calls requests.post() with no timeout parameter, causing the HTTP request to hang indefinitely when Stripe is slow or unresponsive. All five chaos scenarios (timeout, 502_burst, 429_rate_limit) manifest as requests.exceptions.Timeout because the mock vendor simulates these failure modes by delaying responses, and the client has no timeout to bound the wait. The route handler in src/app/routes/charge.py function charge() calls create_payment_intent() with no try/except, so the uncaught Timeout exception propagates up and crashes the request, returning no HTTP response to the caller.
- **SendGrid** (`src/app/clients/sendgrid.py` → `send_email`): timeout_with_retry — The send_email function in src/app/clients/sendgrid.py (lines 30-40) calls requests.post without a timeout parameter, causing indefinite hangs when SendGrid is slow (timeout scenario). It returns response.status_code unchecked, so 429 and 502 vendor errors propagate as normal return values. The notify route in src/app/routes/notify.py (line 25) wraps any code into {"status": "sent", "code": code} with HTTP 200, silently swallowing SendGrid's 429/502 errors (rate limit and burst scenarios). No retry logic exists for transient 429/502 errors.

## Attempted Patches
- None generated

## Failure Reason
```
Agent 6 (Patch Generator) failed: Agent 6 returned invalid JSON for Stripe: Expecting ',' delimiter: line 10 column 3 (char 3183)

Raw:
{
  "vendor": "Stripe",
  "affected_file": "src/app/clients/stripe.py",
  "fix_strategy": "timeout_with_retry",
  "fixed_source": "\"\"\"Stripe API client \u2014 intentionally has no retry, timeout, or error handling.\"\"\"\n\nimport os\nimport time\nimport requests\nfrom requests.exceptions import Timeout, RequestException\n\n\ndef create_payment_intent(amount: int, currency: str, payment_method: str) -> dict:\n    \"\"\"\n    Create and confirm a Stripe PaymentIntent.\n\n    Args:\n        amount: Amount in smallest currency unit (e.g. cents for USD).\n        currency: ISO 4217 currency code (e.g. \"usd\").\n        payment_method: Stripe PaymentMethod ID (e.g. \"pm_card_visa\").\n\n    Returns:\n        Raw Stripe API response as a dict.\n\n    Raises:\n        KeyError: If the response does not contain expected fields (no validation).\n        requests.exceptions.Timeout: Propagates uncaught if Stripe times out.\n    \"\"\"\n    base_url = os.environ.get(\"STRIPE_BASE_URL\", \"https://api.stripe.com\")\n    api_key = os.environ[\"STRIPE_API_KEY\"]\n\n    for attempt in range(2):\n        try:\n            response = requests.post(\n                f\"{base_url}/v1/payment_intents\",\n                auth=(api_key, \"\"),\n                data={\n                    \"amount\": amount,\n                    \"currency\": currency,\n                    \"payment_method\": payment_method,\n                    \"confirm\": True,\n                },\n                timeout=10,\n            )\n            if response.status_code in (429, 502, 503):\n                raise RequestException(f\"Transient error {response.status_code}\")\n            return response.json()\n        except (Timeout, RequestException) as e:\n            if attempt == 0:\n                time.sleep(1)\n                continue\n            return {\"error\": \"timeout\", \"message\": str(e)}\n    return {\"error\": \"timeout\", \"message\": \"Max retries exceeded\"}\n",
  "extra_patches": [
    {
      "affected_file": "src/app/routes/charge.py",
      "fixed_source": "\"\"\"POST /charge \u2014 process a payment via Stripe.\"\"\"\n\nfrom flask import Blueprint, request, jsonify\nfrom app.clients import stripe\n\ncharge_bp = Blueprint(\"charge\", __name__)\n\n\n@charge_bp.route(\"/charge\", methods=[\"POST\"])\ndef charge():\n    \"\"\"\n    Process a payment intent via Stripe.\n\n    Request JSON:\n        amount (int): Amount in cents.\n        currency (str, optional): ISO 4217 code. Defaults to \"usd\".\n        payment_method (str): Stripe PaymentMethod ID.\n\n    Returns:\n        JSON with payment_intent_id and status on success.\n        No error handling \u2014 any Stripe failure propagates as an unhandled exception.\n    \"\"\"\n    data = request.json\n    result = stripe.create_payment_intent(\n        amount=data[\"amount\"],\n        currency=data.get(\"currency\", \"usd\"),\n        payment_method=data[\"payment_method\"],\n    )\n    if \"error\" in result:\n        return jsonify({\"error\": result.get(\"message\", \"vendor error\")}), 503\n    return jsonify({\"payment_intent_id\": result[\"id\"], \"status\": result[\"status\"]})\n"
  ],
  "pr_title": "fix(stripe): add timeout and retry to create_payment_intent",
  "pr_description": "## What Failed\nThe Stripe client called requests.post without a timeout, causing indefinite hangs during vendor slowdowns; the route handler had no error handling so uncaught exceptions crashed the request.\n## Fix Applied\nAdded a 10-second timeout to the HTTP call, wrapped it in a try/except for Timeout and RequestException, and implemented one retry with a 1-second sleep for transient status codes (429, 502, 503). On failure the function returns a structured error dict. The caller now checks for an error key and returns a 503 response.\n## Why This Strategy\ntimeout_with_retry bounds the wait time and recovers from transient vendor blips without adding circuit-breaker complexity, matching the criticality score 95 requirement for aggressive resilience."
}
```

## Recommended Next Steps
Review the diagnoses above and apply the suggested fix strategies manually.
